#+TITLE: Divide and Conquer
#+AUTHOR: Ben Heinze
#+DATE: 2026-07-04
#+STARTUP: noindent
#+OPTIONS: toc:2 num:nil

#+MACRO: hl @@html:<span class="hl-$1">$2</span>@@@@latex:\hl{$1}{$2}@@

# This is used to shrink the pdf page margins
#+LATEX_HEADER: \usepackage[left=0.75in,right=0.75in,top=1in,bottom=1in]{geometry}

# This is used to shrink the spacing between bullet points
#+LATEX_HEADER: \usepackage{enumitem}
#+LATEX_HEADER: \setlist[itemize]{itemsep=2pt, topsep=4pt}

Divide and conquer solves a problem by:

1. *Divide* — split the input into smaller sub-problems.
2. *Conquer* — solve each sub-problem recursively.
3. *Combine* — merge the sub-solutions into a solution to the whole.

The recursion terminates on trivially small sub-problems solved
directly.

#+INCLUDE: "closest-pair.org" :minlevel 2
