#+TITLE: Randomized Algorithms
#+AUTHOR: Ben Heinze
#+DATE: 2026-07-04
#+STARTUP: noindent
#+OPTIONS: toc:2 num:nil

#+MACRO: hl @@html:<span class="hl-$1">$2</span>@@@@latex:\hl{$1}{$2}@@

# This is used to shrink the pdf page margins
#+LATEX_HEADER: \usepackage[left=0.75in,right=0.75in,top=1in,bottom=1in]{geometry}

# This is used to shrink the spacing between bullet points
#+LATEX_HEADER: \usepackage{enumitem}
#+LATEX_HEADER: \setlist[itemize]{itemsep=2pt, topsep=4pt}

Randomization gives simple algorithms with strong /expected/
guarantees. Two flavors show up in this class:

- *Random assignments* — pick each decision uniformly at random and
  bound the expected quality (Max 3-Coloring, Max 3-SAT).
- *Randomized rounding* — solve an LP relaxation and use the LP values
  as probabilities for how to round (Set Cover).

Recall for maximization problems: if the algorithm's expected value
satisfies $E[ALG] \geq (1/\alpha) OPT$, it is an $\alpha$-approximation
in expectation.

#+INCLUDE: "max-3-coloring.org" :minlevel 2
#+INCLUDE: "max-3-sat.org" :minlevel 2
#+INCLUDE: "randomized-rounding-set-cover.org" :minlevel 2
